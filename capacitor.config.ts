import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jenriquelopez.news',
  appName: 'noticias',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
